import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // next/link's <Link> crashes at runtime under this project's vinext
      // setup (confirmed: "Invalid hook call" from a duplicate-React-instance
      // resolution bug — see memory/vinext_next_link_broken.md). Plain <a>
      // tags are the deliberate, working replacement for internal navigation.
      "@next/next/no-html-link-for-pages": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
